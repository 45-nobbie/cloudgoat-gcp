name: CI
on:
  push:
    branches: [ dev, main ]
  pull_request:
    branches: [ dev ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: 'CI placeholder'
        run: echo 'Run build/test steps here'
